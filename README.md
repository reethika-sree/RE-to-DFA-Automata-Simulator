# RE-to-DFA-Automata-Simulator
A web-based interactive tool that converts regular expressions into visual NFA, DFA, and minimized DFA diagrams using Mermaid.js. It also features a built-in string testing simulator to verify matches directly against your automata state machines
